/*--------------------------------*- C++ -*----------------------------------*\
  =========                 |
  \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox
   \\    /   O peration     | Website:  https://openfoam.org
    \\  /    A nd           | Version:  9
     \\/     M anipulation  |
\*---------------------------------------------------------------------------*/
FoamFile
{
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

ddtSchemes
{
    default         Euler;
    // default         CrankNicolson 0.9;
}

gradSchemes
{
    // default         cellMDLimited Gauss linear 1.0;
    // grad(p)         Gauss linear;
    // grad(fw)         Gauss linear;

    default         Gauss linear;

}

divSchemes
{
    default              none;
    // div(grad(p))        Gauss linear;

    // div(phi,fw)            Gauss limitedLinear 0.333;  ,

    // div(phia,Sa)         Gauss vanLeer;
    // div(phib,Sb)         Gauss vanLeer;

    // div(phia,Sa)         Gauss upwind;
    // div(phib,Sb)         Gauss upwind;

    // div(phi,fw)         Gauss linear;
    // div(phi,fw)         Gauss limitedLinear 0.1;

}

laplacianSchemes
{
    // default         Gauss linear orthogonal;

    // default         Gauss linear corrected;

    // default            Gauss linear limited 0.333;

    default         	Gauss linear corrected;

}

interpolationSchemes
{
    default         none;
    K               harmonic;

    // // p               linear;
    // Sa              upwind phia;
    // Sb              upwind phib;
    // magGradP         upwind phi;

    kra	            upwind phia;
    krb         	upwind phib;
    krc         	upwind phic;

    // Fdry            upwind phib;
    // Nca             upwind phi;
    // Fshear          upwind phi;
    // Foil            upwind phic;
    // MRF             upwind phia;

    // kra             QUICK phia;
    // krb             QUICK phib;
    // krc             QUICK phic;

    //kra             vanLeer phia;
    //krb             vanLeer phib;
    //krc             vanLeer phic;

    // kra             limitedLinear phia 1.0;
    // krb             limitedLinear phib 1.0;
    // krc             limitedLinear phic 1.0;

    // kra             TOPUS phia 0.1;
    // krb             TOPUS phib 0.1;
    // krc             TOPUS phic 0.1;
    
    dpcow_dsw       linear;
    dpcow_dsg       linear;       
    dpcgo_dsg       linear;
    dpcgo_dsw       linear;

    flux(Ub)        linear;
    flux(Ua)        linear;
    flux(Uc)        linear;
    flux(U)         linear;
}

snGradSchemes
{
    // default         orthogonal;

    // default            limited 0.333;

    default         corrected;
}

fluxRequired
{
    default         no;
    p           ;
}


// ************************************************************************* //
